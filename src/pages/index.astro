---
import Layout from '../layouts/Layout.astro';
import SectionHeader from '../components/SectionHeader.astro';
import { getPublishedBlogPosts, blogPostUrl } from '../lib/blog';

const posts = await getPublishedBlogPosts();
const recentPosts = posts.slice(0, 3);
---

<Layout title="Soma — An AI agent that grows with you">
  <div class="stars"></div>
  <div class="nebula"></div>

  <main class="container">
    <!-- Hero -->
    <header class="hero">
      <div class="hero-icon">
        <img src="/media/soma-logo-animated.svg" alt="Soma" width="144" height="144" />
      </div>

      <h1 class="title">
        <span class="title-main">Soma</span>
        <span class="beta-badge">beta</span>
      </h1>

      <p class="tagline">An AI agent that grows with you</p>

      <p class="description">
        Memory that persists across sessions. Identity that evolves through use.<br/>
        Built on <a href="https://github.com/badlogic/pi-mono" target="_blank" rel="noopener">Pi</a>. Extended with skills, extensions, and protocols. Yours to shape.
      </p>
    </header>

    <!-- Recent Posts -->
    {recentPosts.length > 0 && (
      <section class="recent-posts">
        {recentPosts.map((post, i) => (
          <a href={blogPostUrl(post)} class="recent-post-card">
            {i === 0 && <span class="recent-post-badge">New</span>}
            <span class="recent-post-title">{post.data.title}</span>
            <span class="recent-post-arrow">→</span>
          </a>
        ))}
      </section>
    )}

    <!-- Quick Start -->
    <section class="quickstart">
      <SectionHeader title="Get Started" />
      <div class="code-block">
        <div class="code-header">
          <span class="code-dot"></span>
          <span class="code-dot"></span>
          <span class="code-dot"></span>
          <span class="code-label">terminal</span>
        </div>
        <div class="code-body">
          <div class="code-line">
            <span class="code-comment"># Install Soma globally</span>
          </div>
          <div class="code-line">
            <span class="code-prompt">$</span>
            <span class="code-cmd">npm i -g meetsoma</span>
            <button class="copy-btn" data-copy="npm i -g meetsoma" title="Copy">
              <svg class="copy-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
              <svg class="check-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="display:none"><polyline points="20 6 9 17 4 12"/></svg>
            </button>
          </div>
          <div class="code-line">
            <span class="code-comment"># Start a session</span>
          </div>
          <div class="code-line">
            <span class="code-prompt">$</span>
            <span class="code-cmd">soma</span>
            <button class="copy-btn" data-copy="soma" title="Copy">
              <svg class="copy-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
              <svg class="check-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="display:none"><polyline points="20 6 9 17 4 12"/></svg>
            </button>
          </div>
          <div class="code-line">
            <span class="code-comment"># Continue where you left off</span>
          </div>
          <div class="code-line">
            <span class="code-prompt">$</span>
            <span class="code-cmd">soma -c</span>
            <button class="copy-btn" data-copy="soma -c" title="Copy">
              <svg class="copy-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
              <svg class="check-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="display:none"><polyline points="20 6 9 17 4 12"/></svg>
            </button>
          </div>
        </div>
      </div>
    </section>

    <script>
      document.querySelectorAll('.copy-btn').forEach(btn => {
        btn.addEventListener('click', async () => {
          const text = (btn as HTMLElement).dataset.copy || '';
          const copyIcon = btn.querySelector('.copy-icon') as HTMLElement;
          const checkIcon = btn.querySelector('.check-icon') as HTMLElement;
          try {
            await navigator.clipboard.writeText(text);
            copyIcon.style.display = 'none';
            checkIcon.style.display = 'block';
            setTimeout(() => { copyIcon.style.display = 'block'; checkIcon.style.display = 'none'; }, 2000);
          } catch {}
        });
      });
    </script>

    <!-- The Four Layers -->
    <section class="layers">
      <SectionHeader title="The Ecosystem" subtitle="Four layers that grow with you" />
      <div class="layers-grid">
        <a href="/ecosystem#extensions" class="layer-card">
          <div class="layer-icon">⟐</div>
          <h3 class="layer-name">Extensions</h3>
          <p class="layer-type">system hooks</p>
          <p class="layer-desc">
            TypeScript plugins that hook into the agent lifecycle. Boot sequences,
            branded UI, auto-flush memory, context warnings. The nervous system.
          </p>
          <div class="layer-examples">
            <code>soma-boot.ts</code>
            <code>soma-header.ts</code>
            <code>soma-statusline.ts</code>
          </div>
        </a>

        <a href="/ecosystem#skills" class="layer-card">
          <div class="layer-icon">◈</div>
          <h3 class="layer-name">Skills</h3>
          <p class="layer-type">knowledge sets</p>
          <p class="layer-desc">
            Markdown instruction files the agent loads on demand. Domain expertise,
            best practices, task-specific knowledge. The learned memory.
          </p>
          <div class="layer-examples">
            <code>logo-creator</code>
            <code>favicon-gen</code>
            <code>remotion</code>
          </div>
        </a>

        <a href="/docs/muscles" class="layer-card">
          <div class="layer-icon">◎</div>
          <h3 class="layer-name">Muscles</h3>
          <p class="layer-type">learned patterns</p>
          <p class="layer-desc">
            Patterns learned from experience — deployment workflows, code conventions,
            API recipes. Heat-tracked and token-budgeted. The muscle memory.
          </p>
          <div class="layer-examples">
            <code>deployment</code>
            <code>pr-workflow</code>
            <code>logo-design</code>
          </div>
        </a>

        <a href="/ecosystem#protocols" class="layer-card">
          <div class="layer-icon">🧬</div>
          <h3 class="layer-name">Protocols</h3>
          <p class="layer-type">behavioral rules</p>
          <p class="layer-desc">
            Drop-in behavioral rules that shape how the agent thinks and operates.
            Used protocols heat up and stay loaded. Unused ones fade. The value system.
          </p>
          <div class="layer-examples">
            <code>breath-cycle</code>
            <code>frontmatter</code>
            <code>heat-tracking</code>
          </div>
        </a>
      </div>
    </section>

    <!-- What Makes Soma Different -->
    <section class="features">
      <SectionHeader title="What Makes Soma Different" />
      <div class="features-grid">
        <div class="feature-card">
          <h3 class="feature-title">Persistent Memory</h3>
          <p class="feature-desc">
            Sessions end. Memory doesn't. Soma flushes what it learns to disk —
            preloads, muscles, context — and picks it back up next time.
          </p>
        </div>
        <div class="feature-card">
          <h3 class="feature-title">Evolving Identity</h3>
          <p class="feature-desc">
            Not pre-configured. Soma's identity file grows through use —
            voice, preferences, working patterns, all discovered, not assigned.
          </p>
        </div>
        <div class="feature-card">
          <h3 class="feature-title">Session Continuity</h3>
          <p class="feature-desc">
            <code>soma -c</code> loads identity plus the last session's preload.
            The agent knows where you left off and what's next.
          </p>
        </div>
        <div class="feature-card">
          <h3 class="feature-title">Adaptive Protocols</h3>
          <p class="feature-desc">
            Behavioral rules that heat up when used and fade when they don't.
            Hot protocols load full context. Cold ones stay available but silent.
            The agent's value system tunes itself.
          </p>
        </div>
        <div class="feature-card">
          <h3 class="feature-title">Built on Pi</h3>
          <p class="feature-desc">
            Soma extends pi's coding agent with custom extensions.
            Everything pi can do, Soma can do — with memory on top.
          </p>
        </div>
        <div class="feature-card">
          <h3 class="feature-title">Open Source</h3>
          <p class="feature-desc">
            Extensions, skills, muscles, protocols — all MIT licensed.
            Build your own agent identity on the same foundation.
          </p>
        </div>
      </div>
    </section>

    <!-- Ecosystem Map -->
    <section class="ecosystem">
      <SectionHeader title="The Soma Ecosystem" link={{ href: "/ecosystem", text: "Learn more →" }} />
      <div class="ecosystem-grid">
        <a href="https://github.com/meetsoma/soma-agent" target="_blank" rel="noopener" class="eco-card">
          <span class="eco-icon">
            <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" width="40" height="40">
              <!-- Soma mascot silhouette — planet + moon, flat icon style -->
              <circle cx="22" cy="28" r="16" fill="var(--accent-bright)"/>
              <circle cx="37" cy="14" r="7" fill="var(--accent-bright)"/>
              <!-- Eyes cutout -->
              <circle cx="17" cy="26" r="3.5" fill="var(--surface-card)"/>
              <circle cx="27" cy="26" r="3.5" fill="var(--surface-card)"/>
              <!-- Pupils -->
              <circle cx="18.2" cy="25.5" r="1.6" fill="var(--accent-bright)"/>
              <circle cx="28.2" cy="25.5" r="1.6" fill="var(--accent-bright)"/>
            </svg>
          </span>
          <span class="eco-name">agent</span>
          <span class="eco-desc">Extensions, skills & protocols</span>
        </a>
        <a href="https://www.npmjs.com/package/meetsoma" target="_blank" rel="noopener" class="eco-card">
          <span class="eco-icon">
            <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" width="40" height="40">
              <!-- npm logo — flat -->
              <rect x="4" y="12" width="40" height="24" rx="3" fill="#CB3837"/>
              <rect x="10" y="18" width="8" height="12" fill="white"/>
              <rect x="13" y="18" width="3" height="9" fill="#CB3837"/>
              <rect x="21" y="18" width="8" height="12" fill="white"/>
              <rect x="24" y="18" width="3" height="9" fill="#CB3837"/>
              <rect x="32" y="18" width="8" height="9" fill="white"/>
              <rect x="35" y="18" width="3" height="6" fill="#CB3837"/>
            </svg>
          </span>
          <span class="eco-name">npm</span>
          <span class="eco-desc">meetsoma — install & run</span>
        </a>
        <a href="/blog" class="eco-card">
          <span class="eco-icon">
            <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" width="40" height="40">
              <!-- Blog / writing icon — quill pen -->
              <path d="M36 8L14 30l-2 8 8-2L42 14a4 4 0 00-6-6z" stroke="var(--accent-bright)" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
              <path d="M32 12l4 4" stroke="var(--accent-bright)" stroke-width="2.5" stroke-linecap="round"/>
              <path d="M12 38h24" stroke="var(--accent-bright)" stroke-width="2.5" stroke-linecap="round" opacity="0.4"/>
            </svg>
          </span>
          <span class="eco-name">blog</span>
          <span class="eco-desc">Souls & Symlinks</span>
        </a>
        <a href="https://github.com/meetsoma" target="_blank" rel="noopener" class="eco-card">
          <span class="eco-icon">
            <svg viewBox="0 0 48 48" fill="none" xmlns="http://www.w3.org/2000/svg" width="40" height="40">
              <!-- GitHub mark — simplified octocat silhouette -->
              <path d="M24 4C12.954 4 4 12.954 4 24c0 8.837 5.731 16.329 13.682 18.976.999.184 1.364-.434 1.364-.964 0-.476-.018-2.05-.026-3.72-5.567 1.209-6.74-2.364-6.74-2.364-.91-2.313-2.222-2.929-2.222-2.929-1.815-1.242.138-1.217.138-1.217 2.009.141 3.065 2.063 3.065 2.063 1.784 3.058 4.681 2.174 5.822 1.662.181-1.293.698-2.175 1.27-2.675-4.443-.505-9.114-2.221-9.114-9.888 0-2.184.78-3.97 2.058-5.369-.206-.505-.892-2.54.196-5.293 0 0 1.679-.538 5.499 2.05A19.128 19.128 0 0124 13.2c1.7.008 3.411.23 5.009.673 3.818-2.588 5.494-2.05 5.494-2.05 1.09 2.753.405 4.788.198 5.293 1.281 1.399 2.055 3.185 2.055 5.369 0 7.687-4.677 9.378-9.132 9.872.718.619 1.357 1.839 1.357 3.706 0 2.676-.024 4.833-.024 5.49 0 .534.36 1.156 1.374.96C38.275 40.323 44 32.834 44 24 44 12.954 35.046 4 24 4z" fill="var(--accent-bright)"/>
            </svg>
          </span>
          <span class="eco-name">github</span>
          <span class="eco-desc">meetsoma org</span>
        </a>
      </div>
    </section>

    <!-- CTA -->
    <section class="cta-section">
      <div class="cta-content">
        <h2 class="cta-title">Start growing</h2>
        <p class="cta-desc">Install Soma. Start a session. Let the memory build.</p>
        <div class="cta-buttons">
          <a href="https://github.com/meetsoma/agent" target="_blank" rel="noopener" class="cta-primary">
            View on GitHub →
          </a>
          <a href="/docs" class="cta-secondary">
            Read the docs
          </a>
        </div>
      </div>
    </section>

  </main>
</Layout>

<style>
  /* ═══════════════════════════════════════════════════════
     Fibonacci Design System
     ═══════════════════════════════════════════════════════
     φ = 1.618 (golden ratio)

     TYPE SCALE (rem) — each step ≈ φ^(n/4)
     --f-xs:   0.813rem  (13px)  — smallest text (labels, badges)
     --f-sm:   0.875rem  (14px)  — secondary text, captions
     --f-base: 1rem      (16px)  — body text
     --f-md:   1.125rem  (18px)  — emphasis, descriptions
     --f-lg:   1.313rem  (21px)  — card headings, sub-headers
     --f-xl:   1.625rem  (26px)  — section headers
     --f-2xl:  2.625rem  (42px)  — page title
     --f-3xl:  4.25rem   (68px)  — hero display

     SPACING SCALE (px) — Fibonacci sequence
     8, 13, 21, 34, 55, 89, 144

     LINE HEIGHTS
     Headings: 1.2
     Body:     1.618 (φ)
     Loose:    1.8
  ═══════════════════════════════════════════════════════ */

  /* Background layers */
  .stars {
    position: fixed;
    inset: 0;
    background-image:
      radial-gradient(1.5px 1.5px at 20px 30px, rgba(228,234,244,0.6), transparent),
      radial-gradient(1px 1px at 80px 60px, rgba(228,234,244,0.4), transparent),
      radial-gradient(1.5px 1.5px at 150px 100px, rgba(124,178,212,0.5), transparent),
      radial-gradient(1px 1px at 220px 40px, rgba(228,234,244,0.3), transparent),
      radial-gradient(1.5px 1.5px at 300px 80px, rgba(232,168,124,0.3), transparent),
      radial-gradient(1px 1px at 360px 130px, rgba(228,234,244,0.5), transparent);
    background-size: 400px 180px;
    animation: twinkle 10s ease-in-out infinite alternate;
    pointer-events: none;
    z-index: 0;
  }

  .nebula {
    position: fixed;
    inset: 0;
    background:
      radial-gradient(ellipse 70% 50% at 15% 20%, rgba(104, 152, 190, 0.08), transparent 50%),
      radial-gradient(ellipse 50% 50% at 85% 30%, rgba(124, 178, 212, 0.06), transparent 50%),
      radial-gradient(ellipse 80% 60% at 50% 85%, rgba(232, 168, 124, 0.04), transparent 50%);
    pointer-events: none;
    z-index: 0;
  }

  @keyframes twinkle {
    0% { opacity: 0.3; }
    100% { opacity: 0.6; }
  }

  .container {
    position: relative;
    z-index: 1;
    max-width: 890px;              /* ~55 * φ^4 */
    margin: 0 auto;
    padding: 55px 21px 34px;       /* fib: 55, 21, 34 */
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  /* ── Hero ── */
  .hero {
    text-align: center;
    margin-bottom: 55px;           /* fib */
    padding-top: 34px;             /* fib */
    animation: fadeUp 0.8s ease-out;
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(21px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .hero-icon {
    width: 144px;                  /* fib */
    height: 144px;
    margin: 0 auto 34px;          /* fib */
    animation: float 5s ease-in-out infinite;
  }

  .hero-icon img {
    width: 100%;
    height: 100%;
    filter: drop-shadow(0 0 21px var(--logo-glow));
    transition: filter 0.3s;
  }

  .hero-icon:hover img {
    filter: drop-shadow(0 0 34px var(--logo-glow-hover));
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-8px); }
  }

  .title {
    position: relative;
    display: inline-flex;
    align-items: flex-start;
    gap: 8px;                      /* fib */
    font-family: var(--font-display);
    font-size: clamp(3.5rem, 12vw, 5.5rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    line-height: 1;
    margin-bottom: 13px;           /* fib */
  }

  .title-main {
    background: linear-gradient(135deg, var(--hero-title-start) 0%, var(--accent-bright) 50%, var(--hero-title-end) 100%);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: gradientShift 8s ease infinite;
  }

  .beta-badge {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.813rem;           /* f-xs */
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--bg-deep);
    background: linear-gradient(135deg, var(--accent-bright) 0%, rgba(124, 178, 212, 0.8) 100%);
    padding: 4px 10px;
    border-radius: 8px;            /* fib */
    margin-top: 0.15em;
    -webkit-text-fill-color: var(--bg-deep);
    animation: badgeBob 3s ease-in-out infinite;
    flex-shrink: 0;
  }

  @keyframes badgeBob {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-5px); }
  }

  @keyframes gradientShift {
    0%, 100% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
  }

  .tagline {
    font-family: var(--font-display);
    font-size: 1.125rem;           /* f-md */
    font-weight: 500;
    color: var(--accent-bright);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    margin-bottom: 21px;           /* fib */
  }

  .description {
    font-size: 1.125rem;           /* f-md */
    color: var(--text-secondary);
    max-width: 610px;
    margin: 0 auto;
    line-height: 1.618;            /* φ */
  }

  .description a {
    color: var(--accent-bright);
    text-decoration: none;
    border-bottom: 1px solid var(--border-accent);
    transition: color 0.2s;
  }

  .description a:hover { color: var(--moon-bright); }

  /* ── Recent Posts ── */
  .recent-posts {
    display: flex;
    flex-direction: column;
    gap: 8px;                      /* fib */
    margin-bottom: 55px;           /* fib */
    animation: fadeUp 0.8s ease-out 0.1s both;
  }

  .recent-post-card {
    display: flex;
    align-items: center;
    gap: 13px;                     /* fib */
    padding: 13px 21px;           /* fib */
    border-radius: 13px;          /* fib */
    border: 1px solid var(--border-subtle);
    background: var(--surface-card);
    text-decoration: none;
    color: var(--text-primary);
    transition: all 0.25s ease;
  }

  .recent-post-card:hover {
    border-color: var(--border-accent);
    transform: translateY(-2px);
    box-shadow: 0 8px 21px var(--shadow-accent-soft);
  }

  .recent-post-badge {
    font-family: var(--font-display);
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--bg-deep);
    background: var(--accent-bright);
    padding: 3px 8px;
    border-radius: 6px;
    flex-shrink: 0;
  }

  .recent-post-title {
    flex: 1;
    font-size: 1rem;
    font-weight: 500;
  }

  .recent-post-arrow {
    color: var(--text-muted);
    font-size: 1.125rem;
    transition: transform 0.2s;
    flex-shrink: 0;
  }

  .recent-post-card:hover .recent-post-arrow { transform: translateX(5px); }

  /* ── Quick Start ── */
  .quickstart {
    margin-bottom: 55px;           /* fib */
    animation: fadeUp 0.8s ease-out 0.2s both;
  }

  .code-block {
    border-radius: 13px;           /* fib */
    border: 1px solid var(--border-subtle);
    background: var(--surface-card-strong);
    overflow: hidden;
  }

  .code-header {
    display: flex;
    align-items: center;
    gap: 8px;                      /* fib */
    padding: 13px 21px;           /* fib */
    border-bottom: 1px solid var(--border-subtle);
  }

  .code-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--surface-interactive);
  }

  .code-label {
    margin-left: 8px;             /* fib */
    font-family: var(--font-mono);
    font-size: 0.813rem;          /* f-xs */
    color: var(--text-muted);
  }

  .code-body {
    padding: 21px;                /* fib */
    font-family: var(--font-mono);
    font-size: 0.938rem;          /* ~15px, between base and sm */
    line-height: 2;
  }

  .code-line {
    display: flex;
    align-items: center;
    gap: 8px;                     /* fib */
  }

  .code-prompt {
    color: var(--accent-bright);
    user-select: none;
  }

  .code-cmd { color: var(--text-primary); }

  .code-comment {
    color: var(--text-muted);
    font-style: italic;
  }

  .copy-btn {
    margin-left: auto;
    background: none;
    border: none;
    color: var(--text-muted);
    cursor: pointer;
    padding: 5px;
    border-radius: 8px;           /* fib */
    transition: color 0.2s, background 0.2s;
    display: flex;
    align-items: center;
  }

  .copy-btn:hover {
    color: var(--accent-bright);
    background: var(--surface-interactive);
  }

  .copy-btn svg { width: 18px; height: 18px; }

  /* ── Four Layers ── */
  .layers {
    margin-bottom: 55px;           /* fib */
    animation: fadeUp 0.8s ease-out 0.3s both;
  }

  .layers-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 21px;                    /* fib */
  }

  .layer-card {
    display: block;
    padding: 34px 21px;           /* fib: 34 vertical, 21 horizontal */
    border-radius: 13px;          /* fib */
    border: 1px solid var(--border-subtle);
    background: var(--surface-card);
    backdrop-filter: blur(8px);
    transition: all 0.25s ease;
    text-decoration: none;
    color: var(--text-primary);
  }

  .layer-card:hover {
    border-color: var(--border-accent);
    transform: translateY(-5px);
    box-shadow: 0 13px 34px var(--shadow-accent-soft);
  }

  .layer-icon {
    font-size: 2.125rem;           /* bumped — visible at distance */
    color: var(--accent-bright);
    margin-bottom: 13px;           /* fib */
    line-height: 1;
  }

  .layer-name {
    font-family: var(--font-display);
    font-size: 1.313rem;          /* f-lg (21px) */
    font-weight: 600;
    margin-bottom: 3px;
    line-height: 1.2;
  }

  .layer-type {
    font-family: var(--font-mono);
    font-size: 0.813rem;          /* f-xs (13px) — was 0.72 */
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: 13px;          /* fib */
  }

  .layer-desc {
    font-size: 1rem;               /* f-base — was 0.9 */
    color: var(--text-secondary);
    line-height: 1.618;           /* φ */
    margin-bottom: 13px;          /* fib */
  }

  .layer-examples {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;                     /* fib */
  }

  .layer-examples code {
    font-family: var(--font-mono);
    font-size: 0.813rem;          /* f-xs — was 0.72 */
    padding: 3px 8px;
    border-radius: 8px;           /* fib */
    background: var(--surface-accent-soft);
    color: var(--accent-bright);
    border: 1px solid rgba(104, 152, 190, 0.15);
  }

  /* ── Features ── */
  .features {
    margin-bottom: 55px;           /* fib */
    animation: fadeUp 0.8s ease-out 0.4s both;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 21px;                    /* fib */
  }

  .feature-card {
    padding: 34px 21px;           /* fib: 34 vertical, 21 horizontal */
    border-radius: 13px;          /* fib */
    border: 1px solid var(--border-subtle);
    background: var(--surface-card);
    transition: all 0.25s ease;
  }

  .feature-card:hover {
    border-color: var(--border-accent);
    transform: translateY(-3px);
    box-shadow: 0 8px 21px var(--shadow-accent-soft);
  }

  .feature-title {
    font-family: var(--font-display);
    font-size: 1.188rem;          /* ~19px, between base and lg */
    font-weight: 600;
    margin-bottom: 8px;           /* fib */
    line-height: 1.2;
  }

  .feature-desc {
    font-size: 1rem;               /* f-base — was 0.9 */
    color: var(--text-secondary);
    line-height: 1.618;           /* φ */
  }

  .feature-desc code {
    font-family: var(--font-mono);
    font-size: 0.875rem;          /* f-sm — was 0.82 */
    padding: 2px 8px;
    border-radius: 5px;
    background: var(--surface-accent-soft);
    color: var(--accent-bright);
  }

  /* ── Ecosystem ── */
  .ecosystem {
    margin-bottom: 55px;           /* fib */
    animation: fadeUp 0.8s ease-out 0.5s both;
  }

  .ecosystem-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 13px;                    /* fib */
  }

  .eco-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;                     /* fib */
    padding: 34px 13px;           /* fib: generous vertical */
    border-radius: 13px;          /* fib */
    border: 1px solid var(--border-subtle);
    background: var(--surface-card);
    text-decoration: none;
    color: var(--text-primary);
    transition: all 0.25s ease;
  }

  .eco-card:hover {
    border-color: var(--border-accent);
    transform: translateY(-5px);
    box-shadow: 0 13px 34px var(--shadow-accent-soft);
  }

  .eco-icon {
    font-size: 2.125rem;           /* matched to layer-icon — was 1.4 */
    color: var(--accent-bright);
    transition: transform 0.25s;
    line-height: 1;
  }

  .eco-card:hover .eco-icon { transform: scale(1.15); }

  .eco-name {
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1.125rem;          /* f-md — was 1rem */
    line-height: 1.2;
  }

  .eco-desc {
    font-size: 0.875rem;          /* f-sm — was 0.78 */
    color: var(--text-muted);
    text-align: center;
    line-height: 1.4;
  }

  /* ── CTA ── */
  .cta-section {
    margin-bottom: 55px;           /* fib */
    animation: fadeUp 0.8s ease-out 0.6s both;
  }

  .cta-content {
    text-align: center;
    padding: 55px 34px;           /* fib */
    border-radius: 21px;          /* fib */
    border: 1px solid var(--border-subtle);
    background: linear-gradient(135deg, rgba(104,152,190,0.06) 0%, var(--surface-card-strong) 50%, rgba(232,168,124,0.04) 100%);
  }

  .cta-title {
    font-family: var(--font-display);
    font-size: 1.625rem;          /* f-xl (26px) */
    font-weight: 600;
    margin-bottom: 8px;           /* fib */
    line-height: 1.2;
  }

  .cta-desc {
    font-size: 1.125rem;          /* f-md */
    color: var(--text-secondary);
    margin-bottom: 34px;          /* fib */
    line-height: 1.618;           /* φ */
  }

  .cta-buttons {
    display: flex;
    gap: 13px;                    /* fib */
    justify-content: center;
    flex-wrap: wrap;
  }

  .cta-primary {
    display: inline-flex;
    align-items: center;
    padding: 13px 34px;           /* fib */
    border-radius: 13px;          /* fib */
    background: linear-gradient(135deg, var(--accent-bright) 0%, var(--accent-dark) 100%);
    color: white;
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1rem;               /* f-base */
    text-decoration: none;
    transition: all 0.25s ease;
  }

  .cta-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 21px var(--shadow-accent-mid);
  }

  .cta-secondary {
    display: inline-flex;
    align-items: center;
    padding: 13px 34px;           /* fib */
    border-radius: 13px;          /* fib */
    border: 1px solid var(--border-subtle);
    background: var(--surface-card);
    color: var(--text-primary);
    font-family: var(--font-display);
    font-weight: 600;
    font-size: 1rem;               /* f-base */
    text-decoration: none;
    transition: all 0.25s ease;
  }

  .cta-secondary:hover {
    border-color: var(--border-accent);
    transform: translateY(-3px);
    box-shadow: 0 8px 21px var(--shadow-accent-soft);
  }

  .sep { color: var(--text-muted); }

  /* ── Responsive ── */
  @media (max-width: 768px) {
    .layers-grid { grid-template-columns: 1fr; }
    .features-grid { grid-template-columns: 1fr; }
    .ecosystem-grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 480px) {
    .container { padding: 34px 13px; }
    .ecosystem-grid { grid-template-columns: 1fr 1fr; }
  }
</style>
