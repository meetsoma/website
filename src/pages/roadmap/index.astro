---
import Layout from '../../layouts/Layout.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import SectionHeader from '../../components/SectionHeader.astro';
import Icon from '../../components/icons/SomaIcons.astro';
import RoadmapTimeline from '../../components/islands/RoadmapTimeline.tsx';
import HeroTitle from '../../components/HeroTitle.astro';

// ─── Recently Shipped ────────────────────────────────────────────────
// Strategic landmark releases, newest first. Shown as an auto-advancing
// hero carousel (one card at a time). Cap: 10 cards. When adding the 11th,
// archive the oldest entry to a separate `archive` array (or move to the
// timeline island below) — NEVER delete. The full history lives in the
// timeline island below this section, so the carousel stays focused on the
// standout stories, not comprehensive coverage.
//
// Hero images: optional. Authoring rule lives at `media/roadmap/README.md`
// + `svg-extract-component.mjs` (extracts a hero component from a blog OG).
// Cards without an OG-paired blog post leave `image` undefined; the carousel
// still renders cleanly (see v0.20.0 / v0.22.0 / v0.24.0 / v0.26.0 below).
const upcoming: { name: string; icon: string; description: string; status: string; statusColor: string; version?: string; date?: string; blogPost?: string; image?: string }[] = [
  {
    name: 'Headless Claude sub-agents — on your subscription, not extra usage',
    icon: 'automations',
    description: 'Spawn a focused child agent that runs through the official Claude CLI — it draws from your subscription plan instead of metered extra usage, the one headless-Claude path that doesn\'t bill overage. `soma claude "<task>"` for a one-shot, or delegate with `model: \'claude-cli/sonnet\'`; read-only by default. The same release restores the agent\'s whole memory layer (a path bug had silently dropped breath/memory/preloads from every prompt for months — now back, with a smoke test to keep it there), makes delegation teachable (the children pattern surfaces in `delegate` help), and hardens the release pipeline so a credential can never reach main and every settings/template change ships its migration.',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.32.0',
    date: '2026-06-15',
    image: '/images/roadmap/headless-claude-subagents.svg',
  },
  {
    name: 'The $0.00 that meant it was flying blind — Fable 5 lands native',
    icon: 'extensions',
    description: 'Anthropic\'s Mythos-class model — 1M context, vision, deep adaptive thinking — is now first-class: `soma --model fable`, with cost tracked per token instead of silently reading zero. The same release ships the meta-workflow cadence (the loop Soma uses to carry work across sessions and improve from its own incidents) as a core protocol you adopt per-project, and a project-trust handler so your `.soma/` settings keep loading transparently under the engine\'s new gating. The preload indicator stopped lying, and idle sessions now save before they close.',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.31.0',
    date: '2026-06-11',
    blogPost: 'the-zero-dollars-that-meant-i-wasnt-running-fable',
    image: '/images/roadmap/fable-native.svg',
  },
  {
    name: 'The body evolved — routing tables, core_rules, leaner preloads',
    icon: 'extensions',
    description: 'Soma\'s body — the files that shape how it thinks — got a structural upgrade after a cross-project audit. A routing table in body.md means a fresh session finds the right context fast; core_rules.md now ships behavioral defaults so good habits load on boot; and _memory.md shed 32% to leave more room for the actual work. The preload fallback in core/preload.ts finally matches the template — so what the code does and what the docs say can no longer drift apart.',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.29.0',
    date: '2026-06-02',
    blogPost: 'the-body-that-grew-around-us',
    image: '/images/roadmap/body-evolution.svg',
  },
  {
    name: 'Browser drives the web — xray, click, fill, wait',
    icon: 'extensions',
    description: 'Four new browser caps ported from the reference CLI. Xray walks the DOM as a compact layout tree so you can survey a page before acting. Click uses Input.dispatchMouseEvent to bypass React Aria isTrusted checks — SPAs just work. Fill injects value + focus/change events. Wait polls for element appearance after SPA hydration. Plus: screenshot writes to /tmp for read-tool display, navigate returns tabId for chaining, and setup messages now list 7 browsers with verified launch flags.',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.28.3',
    date: '2026-06-01',
    image: '/images/roadmap/browser-tools.svg',
  },
  {
    name: 'Stop crashing on compaction',
    icon: 'protocols',
    description: 'Your soma keeps running through compaction. Older extensions that returned bare strings (the pattern `_tool-template.ts` shipped for weeks) now just work — Soma lifts them into the right envelope before they hit Pi. The preload notify goes yellow when stale (`5+ tool calls since save`), green when refreshed — one notification per state change, not every turn.',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.26.0',
    date: '2026-05-07',
    image: '/images/roadmap/compaction-resilience-dominoes.svg',
  },
  {
    name: 'The warning that meant something else',
    icon: 'protocols',
    description: 'Soma checks for updates before each session: `⬆ (c)ontinue   (u)pdate now   (s)kip this version`. One keystroke installs the latest — no remembering `npm i -g meetsoma@latest`. Migrations now run on every boot, so the fix actually lands instead of silently skipping.',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.25.0',
    date: '2026-05-04',
    blogPost: 'the-warning-curtis-saw-every-startup',
    image: '/images/roadmap/preflight-ci-output.svg',
  },
  {
    name: 'Any GitHub repo is a local codebase',
    icon: 'extensions',
    description: 'Survey any OSS repo before you write your version. `soma github tj/commander.js local find module.exports` fetches a tarball in 1–5s, then runs full ripgrep regex, DEF/USE refs, and blast-radius against the cached extract — 12 languages, far past the API search\'s 3. Plus `dev:kanban.*` for batch ticket audits across kanban + git + sessions, and `soma:docs.whats_new` for an agent-facing changelog. (v0.26.1 closed the bundling gap that briefly held local-mode to schema-only.)',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.24.0',
    date: '2026-05-01',
    image: '/images/roadmap/github-tools-chips.svg',
  },
  {
    name: 'Meta-tools land',
    icon: 'extensions',
    description: 'Add tools without busting your prompt cache. Family meta-tools — `soma:agent.*`, `soma:browser.*`, `soma:docs.*`, `soma:body.*`, `soma:focus.*`, `soma:new.*`, `soma:terminals.*` — register via a route bus, not as top-level Pi tools. Iterating extensions no longer invalidates Anthropic\'s cache (the per-edit tax is gone). `soma bridge` CLI ships alongside for somaverse pairing.',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.22.0',
    date: '2026-04-25',
    image: '/images/roadmap/meta-tools-mascot.svg',
  },
  {
    name: 'Children Control Panel + tmux',
    icon: 'automations',
    description: 'Background delegation for every user. `delegate(background:true)` spawns children in detached tmux sessions; `children list/tail/steer/kill/harvest` is the full control surface. Attach-on-demand via `tmux attach` to watch a child work. cmux stays as the dev-mode fallback.',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.21.1',
    date: '2026-04-22',
    blogPost: 'four-commands-after-an-update',
    image: '/images/roadmap/children-terminal.svg',
  },
  {
    name: 'Cache Economics',
    icon: 'protocols',
    description: 'Cached prompt prefix shrunk ~25% per boot. Recent Ancestry auto-injects the last three session-log entries so the next-self sees continuity without paying the cache cost. Declarative migration replay makes settings-backfill data-driven.',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.21.0',
    date: '2026-04-22',
    image: '/images/roadmap/cache-economics-mascot.svg',
  },
  {
    name: 'Cache Stickiness',
    icon: 'protocols',
    description: 'Iterating on your setup is free. Reload your extensions as often as you want — your session’s cached memory stays warm, so there’s no dollar cost for rapid iteration. Body edits apply on your terms with a new `/rebuild` command.',
    status: 'Shipped',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.20.3',
    date: '2026-04-20',
    blogPost: 'why-your-claude-bill-spiked',
    image: '/images/roadmap/cache-stickiness-brokenclaw.svg',
  },
  {
    name: 'Somaverse',
    icon: 'extensions',
    description: 'Visual workspace at somaverse.ai — 33 plugins, 28 agent tools. `soma login` pairs your agent. Hub relay routes data through your machine, never stores it. Per-user isolation with Argon2 device keys. Your browser is the UI, your machine is the brain.',
    status: 'Live',
    statusColor: 'var(--green-bright, #4ade80)',
    version: 'v0.12.0',
    date: '2026-03-20',
    blogPost: 'the-operating-system',
    image: '/images/roadmap/somaverse-terminal.svg',
  },
];

function formatDate(d?: string): string {
  if (!d) return '';
  const date = new Date(d);
  if (Number.isNaN(+date)) return d;
  return date.toLocaleDateString('en-US', { month: 'short', day: 'numeric' });
}
---

<Layout
  title="Roadmap — σ"
  description="Where Soma has been and where it's going. Timeline of shipped features and what's next."
  canonicalUrl="https://soma.gravicity.ai/roadmap"
  ogImage="/images/roadmap/og-roadmap.png"
>
  <main class="container">
    <header class="hero">
      <Breadcrumbs items={[{ label: 'Roadmap', href: '/roadmap' }]} />
      <HeroTitle text="Roadmap" align="center" />
      <p class="subtitle">
        Where it's going. Where it's been.
      </p>
    </header>

    <!-- Recently Shipped (auto-advancing hero carousel) -->
    <section class="section">
      <SectionHeader title="Recently Shipped" />
      <div
        class="hero-carousel"
        data-carousel
        data-count={upcoming.length}
        aria-roledescription="carousel"
        aria-label="Recently shipped features"
      >
        <!-- Header strip: counter + nav -->
        <div class="hero-top">
          <span class="hero-counter">
            <span data-carousel-index>1</span> / <span>{upcoming.length}</span>
          </span>
          <div class="hero-nav">
            <button class="hero-btn" data-carousel-prev aria-label="Previous feature">‹</button>
            <button class="hero-btn" data-carousel-next aria-label="Next feature">›</button>
          </div>
        </div>

        <!-- Slides viewport -->
        <div class="hero-viewport" data-carousel-viewport>
          {upcoming.map((feature, i) => (
            <article
              class="hero-slide"
              data-carousel-slide
              data-index={i}
              aria-roledescription="slide"
              aria-label={`${i + 1} of ${upcoming.length}: ${feature.name}`}
              aria-hidden={i === 0 ? 'false' : 'true'}
            >
              {/* Oversized version watermark — drifts behind content */}
              {feature.version && (
                <span class="hero-watermark" aria-hidden="true">{feature.version}</span>
              )}

              {/* Decorative art layer — OG image from the related blog post, */}
              {/* clipped + faded so it tints the background without competing. */}
              {feature.image && (
                <img
                  class="hero-art"
                  src={feature.image}
                  alt=""
                  loading="lazy"
                  aria-hidden="true"
                />
              )}

              <div class="hero-content">
                <div class="hero-head">
                  <span class="hero-icon"><Icon name={feature.icon as any} size={28} /></span>
                  <div class="hero-meta">
                    {feature.version && <span class="hero-version">{feature.version}</span>}
                    {feature.date && <span class="hero-date">{formatDate(feature.date)}</span>}
                    <span
                      class="hero-status"
                      style={`color: ${feature.statusColor}; border-color: ${feature.statusColor}`}
                    >
                      {feature.status}
                    </span>
                  </div>
                </div>

                <h3 class="hero-title">{feature.name}</h3>

                <p class="hero-desc" set:html={feature.description} />

                {feature.blogPost && (
                  <a class="hero-link" href={`/blog/${feature.blogPost}`}>
                    <span aria-hidden="true">›</span>
                    <span>read the story</span>
                  </a>
                )}
              </div>
            </article>
          ))}
        </div>

        <!-- Dot indicators -->
        <div class="hero-controls">
          <div class="hero-dots" role="tablist" aria-label="Select feature">
            {upcoming.map((feature, i) => (
              <button
                class="hero-dot"
                data-carousel-dot
                data-index={i}
                role="tab"
                aria-selected={i === 0 ? 'true' : 'false'}
                aria-label={`Go to feature ${i + 1}: ${feature.name}`}
              ></button>
            ))}
          </div>
        </div>
      </div>
    </section>

    <script>
      // ─── Auto-advancing carousel ─────────────────────────────────────
      // One slide visible at a time; crossfade between slides. Auto-advance
      // every ADVANCE_MS (pauses on pointer hover + when document is hidden).
      // Accessible: prev/next buttons, dot tabs, ArrowLeft/ArrowRight keys
      // (when carousel is focused-within), aria-hidden on non-active slides.
      (function () {
        const ADVANCE_MS = 9000;
        const carousel = document.querySelector('[data-carousel]') as HTMLElement | null;
        if (!carousel) return;

        const slides = Array.from(carousel.querySelectorAll<HTMLElement>('[data-carousel-slide]'));
        const dots = Array.from(carousel.querySelectorAll<HTMLElement>('[data-carousel-dot]'));
        const indexEl = carousel.querySelector<HTMLElement>('[data-carousel-index]');
        const prevBtn = carousel.querySelector<HTMLElement>('[data-carousel-prev]');
        const nextBtn = carousel.querySelector<HTMLElement>('[data-carousel-next]');
        const count = slides.length;
        if (!count) return;

        let current = 0;
        let timer: number | null = null;
        let paused = false;

        function setActive(i: number) {
          current = ((i % count) + count) % count;
          slides.forEach((s, idx) => {
            const active = idx === current;
            s.classList.toggle('is-active', active);
            s.setAttribute('aria-hidden', active ? 'false' : 'true');
          });
          dots.forEach((d, idx) => {
            d.setAttribute('aria-selected', idx === current ? 'true' : 'false');
          });
          if (indexEl) indexEl.textContent = String(current + 1);
        }

        function schedule() {
          if (timer !== null) window.clearInterval(timer);
          timer = window.setInterval(() => {
            if (!paused) setActive(current + 1);
          }, ADVANCE_MS);
        }

        // ── Events ────────────────────────────────────────────────────
        dots.forEach((d) => {
          d.addEventListener('click', () => {
            const i = Number(d.getAttribute('data-index') || 0);
            setActive(i);
            schedule();
          });
        });
        prevBtn?.addEventListener('click', () => {
          setActive(current - 1);
          schedule();
        });
        nextBtn?.addEventListener('click', () => {
          setActive(current + 1);
          schedule();
        });

        // Pause on hover/focus, resume on leave/blur
        carousel.addEventListener('pointerenter', () => { paused = true; });
        carousel.addEventListener('pointerleave', () => { paused = false; });

        // Keyboard nav when the carousel or any of its children has focus
        carousel.addEventListener('keydown', (e) => {
          if (e.key === 'ArrowLeft') { setActive(current - 1); schedule(); }
          else if (e.key === 'ArrowRight') { setActive(current + 1); schedule(); }
        });

        // Pause when the tab goes hidden (no background burn)
        document.addEventListener('visibilitychange', () => {
          paused = document.hidden;
        });

        setActive(0);
        schedule();
      })();
    </script>

    <!-- In Progress -->
    <section class="section">
      <SectionHeader title="Shipping Soon" />
      <div class="progress-list">
        <div class="progress-item">
          <span class="progress-dot active"></span>
          <div>
            <strong>Somaverse public beta</strong>
            <p>Open registration, team workspaces, pane drag-and-drop. Visual workspace for your agent — already live at somaverse.ai.</p>
          </div>
        </div>
        <div class="progress-item">
          <span class="progress-dot active"></span>
          <div>
            <strong>Niche templates</strong>
            <p>Next.js, Rust, Python ML shipped. Go, Laravel, Astro, React Native, Monorepo planned. Each template shapes the agent for your stack.</p>
          </div>
        </div>
        <div class="progress-item">
          <span class="progress-dot active"></span>
          <div>
            <strong>CLI convergence</strong>
            <p>Reduce wrapping around the Pi engine. Let upstream flags pass through cleanly. Persistent model switching already shipped — more commands coming.</p>
          </div>
        </div>
        <div class="progress-item">
          <span class="progress-dot"></span>
          <div>
            <strong>Recall memory</strong>
            <p>Graph-backed persistent memory. Cross-session knowledge. The agent remembers patterns, decisions, and context across projects.</p>
          </div>
        </div>
        <div class="progress-item">
          <span class="progress-dot"></span>
          <div>
            <strong>Content versioning</strong>
            <p>Know when hub protocols update. Diff before upgrading. Never lose your customizations to a hub sync.</p>
          </div>
        </div>
        <div class="progress-item">
          <span class="progress-dot"></span>
          <div>
            <strong>Async agent comms</strong>
            <p>Soma reaches you when decisions are needed. File-based inbox already works. Push notifications coming.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Shipped Timeline -->
    <section class="section">
      <SectionHeader title="Shipped" />
      <RoadmapTimeline client:load />
    </section>

    <!-- Changelog link -->
    <div class="changelog-link">
      <p>
        Every commit, every fix, every feature — the full history lives in the
        <a href="/changelog">changelog</a>.
      </p>
    </div>

    <!-- Voting Teaser -->
    <div class="voting-teaser">
      <p>
        <strong>Feature voting coming soon.</strong> Run <code>soma vote &lt;feature&gt;</code> to 
        shape what ships next.
      </p>
    </div>
  </main>
</Layout>

<style>
  .container {
    position: relative;
    z-index: 1;
    max-width: 890px;
    margin: 0 auto;
    padding: var(--space-3xl) var(--space-xl) var(--space-2xl);
    min-height: 100vh;
  }

  /* ── Hero ── */
  .hero {
    text-align: center;
    margin-bottom: var(--section-gap);
    animation: fadeUp 0.8s ease-out;
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(21px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 6vw, 2.625rem);
    font-weight: 800;
    margin: var(--space-md) 0;
    background: linear-gradient(135deg, var(--hero-title-start) 0%, var(--hero-title-end) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    font-family: var(--font-body);
    font-size: var(--text-lg);
    color: var(--ink-soft);
    max-width: 34rem;
    margin: var(--space-md) auto 0;
    line-height: var(--leading-relaxed);
  }

  .section {
    margin-bottom: var(--section-gap);
  }

  /* ─── Hero carousel ──────────────────────────────────────────────── */
  .hero-carousel {
    position: relative;
    padding: var(--space-lg) var(--space-xl) var(--space-xl);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    background: var(--surface-card);
    backdrop-filter: blur(8px);
    overflow: hidden;
    animation: fadeUp 0.6s ease-out both;
  }

  .hero-carousel::before {
    /* Soft accent wash on the top edge, like the "latest" feel on every slide */
    content: "";
    position: absolute;
    inset: 0 0 auto 0;
    height: 2px;
    background: linear-gradient(
      90deg,
      transparent 0%,
      var(--accent-bright, #7cb2d4) 50%,
      transparent 100%
    );
    opacity: 0.55;
  }

  .hero-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: var(--space-md);
    position: relative;
    z-index: 2;
  }

  .hero-counter {
    font-family: var(--font-mono);
    font-size: 0.78rem;
    color: var(--text-tertiary, #6e7681);
    letter-spacing: 0.06em;
  }
  .hero-counter [data-carousel-index] {
    color: var(--accent-bright, #7cb2d4);
    font-weight: 500;
  }

  .hero-nav {
    display: flex;
    gap: 0.35rem;
  }
  .hero-btn {
    width: 32px;
    height: 32px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: transparent;
    color: var(--ink-soft);
    font-size: 1.25rem;
    line-height: 1;
    cursor: pointer;
    transition: all 0.15s ease;
    padding: 0;
  }
  .hero-btn:hover {
    border-color: var(--accent, #7cb2d4);
    color: var(--accent-bright, #7cb2d4);
  }
  .hero-btn:active {
    transform: scale(0.94);
  }

  /* Slides viewport — stacked; active one fades in over the others */
  .hero-viewport {
    position: relative;
    min-height: 340px;
  }

  .hero-slide {
    position: absolute;
    inset: 0;
    opacity: 0;
    transform: translateY(8px);
    pointer-events: none;
    transition: opacity 0.55s ease, transform 0.55s ease;
  }
  .hero-slide.is-active {
    opacity: 1;
    transform: translateY(0);
    pointer-events: auto;
  }

  /* Oversized version watermark drifting behind content */
  .hero-watermark {
    position: absolute;
    right: -0.05em;
    bottom: -0.25em;
    font-family: var(--font-mono);
    font-size: clamp(7rem, 18vw, 13rem);
    font-weight: 700;
    line-height: 0.85;
    letter-spacing: -0.04em;
    color: rgba(124, 178, 212, 0.035);
    pointer-events: none;
    user-select: none;
    white-space: nowrap;
    z-index: 0;
  }

  /* Decorative art layer — a single hero component extracted from the
   * related blog's OG SVG (via scripts/svg-extract-component.mjs).
   * Sits in the right ~40% of the card. Intentionally allowed to crop
   * past the right edge: cutoff signals this is a slice from a larger
   * story, not a contained image. Left edge fades into the prose. */
  .hero-art {
    position: absolute;
    top: 50%;
    right: 0;
    transform: translateY(-50%);
    /* Component lives in the right pane, oversized so it crops */
    width: 52%;
    max-width: 560px;
    height: auto;
    /* Cap height so tall components (the mascot) don't blow the card */
    max-height: 88%;
    object-fit: contain;
    object-position: right center;
    opacity: 0.7;
    pointer-events: none;
    user-select: none;
    z-index: 0;
    mask-image: linear-gradient(
      90deg,
      transparent 0%,
      black 22%,
      black 100%
    );
    -webkit-mask-image: linear-gradient(
      90deg,
      transparent 0%,
      black 22%,
      black 100%
    );
    filter: saturate(0.95);
    animation: artDrift 18s ease-in-out infinite;
  }
  @keyframes artDrift {
    0%, 100% { transform: translate(0, -50%); }
    50%      { transform: translate(-0.8%, -51%); }
  }

  .hero-content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
    /* Content column is full-width structurally — individual children
     * (.hero-desc, .hero-link) clamp themselves to ~3/5 so the art
     * layer on the right stays breathable. The title row escapes that
     * clamp so SHIPPED always floats right of the title. */
    width: 100%;
  }

  .hero-head {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    flex-wrap: wrap;
  }

  .hero-icon {
    display: flex;
    align-items: center;
    color: var(--accent);
  }

  .hero-meta {
    display: flex;
    align-items: baseline;
    gap: var(--space-sm);
    flex: 1;
    flex-wrap: wrap;
    font-family: var(--font-mono);
    row-gap: 0.35rem;
  }

  /* (hero-titlebar removed — status moved into hero-meta) */
  .hero-version {
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--accent-bright, #7cb2d4);
    letter-spacing: 0.01em;
  }
  .hero-date {
    font-size: 0.82rem;
    color: var(--text-tertiary, #6e7681);
  }
  .hero-date::before {
    content: "·";
    margin-right: var(--space-sm);
    color: var(--text-tertiary, #6e7681);
  }

  /* Status pill — small, sits inline with version · date in the meta row. */
  .hero-status {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    padding: 0.12em 0.6em;
    border-radius: var(--radius-full);
    border: 1px solid;
    background: transparent;
    flex-shrink: 0;
    line-height: 1.5;
  }
  /* Pill sits after date; flex gap on .hero-meta provides the visual break. */
  .hero-meta .hero-status {
    margin-left: 0.1rem;
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 2.5vw, 2rem);
    font-weight: 600;
    color: var(--ink);
    margin: 0;
    line-height: var(--leading-tight);
    letter-spacing: -0.015em;
  }

  .hero-desc {
    font-family: var(--font-body);
    font-size: clamp(0.95rem, 1.2vw, 1.05rem);
    color: var(--ink-soft);
    line-height: var(--leading-relaxed);
    margin: 0;
    /* Prose stops at ~3/5 so the hero art has room on the right */
    max-width: 60%;
  }
  .hero-desc :global(code) {
    font-family: var(--font-mono);
    font-size: 0.88em;
    color: var(--ink);
    padding: 0.1em 0.4em;
    background: rgba(0, 0, 0, 0.25);
    border-radius: 4px;
  }

  .hero-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4em;
    margin-top: var(--space-sm);
    font-size: 0.92rem;
    color: var(--accent, #7cb2d4);
    text-decoration: none;
    font-weight: 500;
    transition: transform 0.2s ease, color 0.15s;
    align-self: flex-start;
    max-width: 60%;
  }
  .hero-link:hover {
    color: var(--accent-bright, #8fc4e6);
    transform: translateX(3px);
  }
  .hero-link span:first-child {
    font-family: var(--font-mono);
    font-size: 1.2em;
    line-height: 1;
    opacity: 0.7;
  }

  /* Dots + progress bar */
  .hero-controls {
    display: flex;
    flex-direction: column;
    gap: var(--space-sm);
    margin-top: var(--space-lg);
    position: relative;
    z-index: 2;
  }

  .hero-dots {
    display: flex;
    gap: 0.5rem;
    justify-content: center;
  }

  .hero-dot {
    width: 8px;
    height: 8px;
    border: none;
    padding: 0;
    border-radius: 50%;
    background: var(--border-subtle, #30363d);
    cursor: pointer;
    transition: all 0.2s ease;
  }
  .hero-dot:hover {
    background: var(--text-tertiary, #6e7681);
  }
  .hero-dot[aria-selected="true"] {
    background: var(--accent-bright, #7cb2d4);
    width: 24px;
    border-radius: 4px;
  }

  /* Respect reduced-motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .hero-slide {
      transition: opacity 0.1s linear;
      transform: none !important;
    }
    .hero-art {
      animation: none !important;
    }
  }

  @media (max-width: 720px) {
    .hero-carousel {
      padding: var(--space-md) var(--space-lg) var(--space-lg);
    }
    .hero-viewport {
      min-height: 380px;
    }
    .hero-watermark {
      font-size: 7rem;
    }
    .hero-head {
      gap: 0.65rem;
    }
    .hero-meta {
      flex: 1 0 100%;
      order: 3;
    }
    /* On narrow screens the visual drops behind the text at low opacity.
     * Description relaxes to full width since the art is no longer a
     * side-by-side peer — it's a background tint. */
    .hero-desc, .hero-link {
      max-width: 100%;
    }
    .hero-art {
      width: 80%;
      max-height: 60%;
      right: -10%;
      opacity: 0.18;
    }
    .hero-titlebar {
      gap: 0.65rem;
    }
  }

  /* ── Changelog Link ── */
  .changelog-link {
    margin-bottom: var(--space-xl);
    text-align: center;
  }

  .changelog-link p {
    font-family: var(--font-body);
    font-size: var(--body-text);
    color: var(--ink-muted);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .changelog-link a {
    color: var(--accent);
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 0.2s ease;
  }

  .changelog-link a:hover {
    border-bottom-color: var(--accent);
  }

  /* ── Voting Teaser ── */
  .voting-teaser {
    margin-bottom: var(--section-gap);
    padding: var(--space-xl);
    background: var(--surface-accent-soft);
    border: 1px solid var(--border-accent);
    border-radius: var(--radius-lg);
    text-align: center;
  }

  .voting-teaser p {
    font-family: var(--font-body);
    font-size: var(--body-text);
    color: var(--ink-soft);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .voting-teaser code {
    font-family: var(--font-mono);
    font-size: var(--label-text);
    color: var(--accent);
    background: var(--surface-interactive);
    padding: 2px var(--space-md);
    border-radius: var(--radius-sm);
  }

  /* ── Shipping Soon ── */
  .progress-list {
    display: flex;
    flex-direction: column;
    gap: var(--space-md);
  }

  .progress-item {
    display: flex;
    gap: var(--space-lg);
    padding: var(--space-lg) var(--space-xl);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    background: var(--surface-card);
    box-shadow: var(--shadow-elevated);
    transition: border-color 0.2s ease;
  }

  .progress-item:hover {
    border-color: var(--border-accent);
  }

  .progress-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--border);
    margin-top: 8px;
    flex-shrink: 0;
  }

  .progress-dot.active {
    background: var(--accent);
    box-shadow: 0 0 6px var(--accent);
  }

  .progress-dot.shipped {
    background: var(--promo);
    box-shadow: 0 0 6px var(--promo);
  }

  .progress-item strong {
    font-family: var(--font-display);
    font-size: var(--body-text);
    color: var(--ink);
    display: block;
    margin-bottom: var(--space-xs);
  }

  .progress-item p {
    font-size: var(--body-secondary);
    color: var(--ink-muted);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  @media (max-width: 768px) {
    .upcoming-grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 640px) {
    .container { padding: var(--space-2xl) var(--space-lg) var(--space-xl); }
  }
</style>

<style is:global>
  /* ── Timeline ── */
  .timeline {
    position: relative;
    padding-left: var(--space-2xl);
  }

  .timeline::before {
    content: '';
    position: absolute;
    left: 7px;
    top: calc(var(--space-xl) + 8px);
    bottom: 5px;
    width: 2px;
    background: linear-gradient(to bottom, var(--border-accent), var(--border));
  }

  .timeline-entry {
    position: relative;
    margin-bottom: var(--space-3xl);
    padding: var(--space-xl);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    background: var(--surface-card);
    transition: all 0.25s ease;
    animation: fadeUp 0.6s ease-out both;
  }

  .timeline-entry:hover {
    border-color: var(--border-accent);
    box-shadow: 0 8px 24px var(--shadow-accent-soft);
  }

  .timeline-entry:first-child {
    border-color: var(--border-accent);
  }

  .timeline-dot {
    position: absolute;
    left: calc(-1 * var(--space-2xl) + 1px);
    top: calc(var(--space-xl) + 3px);
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: var(--bg);
    border: 2px solid var(--accent);
    z-index: 1;
  }

  .timeline-entry:first-child .timeline-dot {
    background: var(--accent);
    box-shadow: 0 0 8px var(--accent);
  }

  .timeline-head {
    display: flex;
    align-items: baseline;
    gap: var(--space-lg);
    flex-wrap: wrap;
    margin-bottom: var(--space-lg);
  }

  .timeline .version {
    font-family: var(--font-mono);
    font-size: var(--text-lg);
    color: var(--accent);
    font-weight: 700;
  }

  .timeline .label {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--ink);
    margin: 0;
    line-height: var(--leading-tight);
  }

  .timeline .date {
    font-family: var(--font-mono);
    font-size: var(--label-text);
    color: var(--ink-muted);
    margin-left: auto;
  }

  .timeline-items {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: var(--space-sm);
  }

  .timeline-items li {
    font-family: var(--font-body);
    font-size: var(--body-text);
    color: var(--ink-soft);
    line-height: var(--leading-relaxed);
    padding: var(--space-xs) 0;
    padding-left: var(--space-xl);
    position: relative;
  }

  .timeline-items li::before {
    content: '';
    position: absolute;
    left: var(--space-md);
    top: 0.7em;
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: var(--accent);
    opacity: 0.5;
  }

  .timeline-entry.latest {
    border-color: var(--border-accent);
    background: linear-gradient(135deg, var(--surface-card) 0%, rgba(104, 152, 190, 0.04) 100%);
  }

  .latest-badge {
    font-family: var(--font-mono);
    font-size: var(--badge-text);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--bg);
    background: var(--accent);
    padding: var(--space-xs) var(--space-md);
    border-radius: var(--radius-full);
    line-height: 1;
  }

  .timeline-loading,
  .timeline-error {
    text-align: center;
    padding: var(--space-2xl);
    color: var(--ink-muted);
    font-family: var(--font-body);
  }

  @media (max-width: 640px) {
    .timeline { padding-left: var(--space-xl); }
    .timeline-dot { left: calc(-1 * var(--space-xl) - 4px); }
    .timeline-entry { padding: var(--space-lg); }
    .timeline-head { flex-direction: column; gap: var(--space-xs); }
    .timeline .date { margin-left: 0; }
  }
</style>
