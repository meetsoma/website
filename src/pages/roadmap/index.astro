---
import Layout from '../../layouts/Layout.astro';
import Breadcrumbs from '../../components/Breadcrumbs.astro';
import SectionHeader from '../../components/SectionHeader.astro';
import Icon from '../../components/icons/SomaIcons.astro';
import RoadmapTimeline from '../../components/islands/RoadmapTimeline.tsx';

const upcoming: { name: string; icon: string; description: string; status: string; statusColor: string }[] = [
  {
    name: 'Content Dependencies',
    icon: 'protocols',
    description: 'Install a muscle and its required protocols auto-resolve. requires/enhances/conflicts frontmatter for all AMPS content. Dependency graph on hub pages.',
    status: 'In Progress',
    statusColor: 'var(--warm-bright)',
  },
  {
    name: 'AMPS Automations v2',
    icon: 'automations',
    description: 'Trigger-based flows that chain actions — session start, post-commit, pre-deploy. Spawns tuned agents for end-to-end workflows.',
    status: 'In Progress',
    statusColor: 'var(--warm-bright)',
  },
  {
    name: '/share Command',
    icon: 'scripts',
    description: 'Publish protocols, muscles, and skills from your .soma/ directly to the community hub. Validates, strips private data, opens a PR — auto-merges for trusted contributors.',
    status: 'Planned',
    statusColor: 'var(--accent-bright)',
  },
  {
    name: 'Framework Compatibility',
    icon: 'extensions',
    description: 'Extensions that read CLAUDE.md, AGENTS.md, .cursorrules — layer Soma memory on top of any existing setup. Drop in and it just works.',
    status: 'Planned',
    statusColor: 'var(--accent-bright)',
  },
  {
    name: 'Bundle Builder',
    icon: 'templates',
    description: 'Multi-select on the hub — pick protocols, muscles, extensions. Generate a soma-bundle.json. One command spawns a fully configured agent anywhere.',
    status: 'Exploring',
    statusColor: 'var(--text-muted)',
  },
  {
    name: 'AI Content Screening',
    icon: 'skills',
    description: 'Haiku-powered review of community submissions — injection scanning, quality scoring, automatic tier classification. The hub moderates itself.',
    status: 'Exploring',
    statusColor: 'var(--text-muted)',
  },
];
---

<Layout
  title="Roadmap — Soma"
  description="Where Soma has been and where it's going. Timeline of shipped features and what's next."
  canonicalUrl="https://soma.gravicity.ai/roadmap"
>
  <main class="container">
    <header class="hero">
      <Breadcrumbs items={[{ label: 'Roadmap', href: '/roadmap' }]} />
      <h1 class="title">Roadmap</h1>
      <p class="subtitle">
        Where Soma has been. Where it's going.
      </p>
    </header>

    <!-- What's Next -->
    <section class="section">
      <SectionHeader title="What's Next" />
      <div class="upcoming-grid">
        {upcoming.map((feature, i) => (
          <div class="feature-card" style={`animation-delay: ${i * 0.08}s`}>
            <div class="feature-head">
              <span class="feature-icon"><Icon name={feature.icon as any} size={22} /></span>
              <h3 class="feature-name">{feature.name}</h3>
              <span class="feature-status" style={`color: ${feature.statusColor}; border-color: ${feature.statusColor}`}>
                {feature.status}
              </span>
            </div>
            <p class="feature-desc">{feature.description}</p>
          </div>
        ))}
      </div>
    </section>

    <!-- Shipped Timeline -->
    <section class="section">
      <SectionHeader title="Shipped" />
      <RoadmapTimeline client:load />
    </section>

    <!-- Voting Teaser -->
    <div class="voting-teaser">
      <p>
        <strong>Feature voting coming soon.</strong> Run <code>soma vote &lt;feature&gt;</code> to 
        shape what ships next.
      </p>
    </div>
  </main>
</Layout>

<style>
  .container {
    position: relative;
    z-index: 1;
    max-width: 890px;
    margin: 0 auto;
    padding: var(--space-3xl) var(--space-xl) var(--space-2xl);
    min-height: 100vh;
  }

  /* ── Hero ── */
  .hero {
    text-align: center;
    margin-bottom: var(--section-gap);
    animation: fadeUp 0.8s ease-out;
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(21px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .title {
    font-family: var(--font-display);
    font-size: clamp(2rem, 6vw, 2.625rem);
    font-weight: 700;
    margin: var(--space-md) 0;
    background: linear-gradient(135deg, var(--hero-title-start) 0%, var(--hero-title-end) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .subtitle {
    font-family: var(--font-body);
    font-size: var(--text-lg);
    color: var(--text-secondary);
    max-width: 34rem;
    margin: var(--space-md) auto 0;
    line-height: var(--leading-relaxed);
  }

  .section {
    margin-bottom: var(--section-gap);
  }

  /* ── Upcoming Grid ── */
  .upcoming-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--grid-gap);
  }

  .feature-card {
    padding: var(--card-pad-y) var(--card-pad-x);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border-subtle);
    background: var(--surface-card);
    backdrop-filter: blur(8px);
    transition: all 0.25s ease;
    animation: fadeUp 0.6s ease-out both;
  }

  .feature-card:hover {
    border-color: var(--border-accent);
    transform: translateY(-3px);
    box-shadow: 0 13px 34px var(--shadow-accent-soft);
  }

  .feature-head {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-lg);
  }

  .feature-icon {
    display: flex;
    align-items: center;
    color: var(--accent-bright);
  }

  .feature-name {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: 600;
    color: var(--text-primary);
    flex: 1;
    margin: 0;
    line-height: var(--leading-tight);
  }

  .feature-status {
    font-family: var(--font-mono);
    font-size: var(--badge-text);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    padding: var(--space-xs) var(--space-md);
    border-radius: var(--radius-full);
    border: 1px solid;
    background: transparent;
    flex-shrink: 0;
  }

  .feature-desc {
    font-family: var(--font-body);
    font-size: var(--body-text);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  /* ── Voting Teaser ── */
  .voting-teaser {
    margin-bottom: var(--section-gap);
    padding: var(--space-xl);
    background: var(--surface-accent-soft);
    border: 1px solid var(--border-accent);
    border-radius: var(--radius-lg);
    text-align: center;
  }

  .voting-teaser p {
    font-family: var(--font-body);
    font-size: var(--body-text);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    margin: 0;
  }

  .voting-teaser code {
    font-family: var(--font-mono);
    font-size: var(--label-text);
    color: var(--accent-bright);
    background: var(--surface-interactive);
    padding: 2px var(--space-md);
    border-radius: var(--radius-sm);
  }

  @media (max-width: 768px) {
    .upcoming-grid { grid-template-columns: 1fr; }
  }

  @media (max-width: 640px) {
    .container { padding: var(--space-2xl) var(--space-lg) var(--space-xl); }
  }
</style>

<style is:global>
  /* ── Timeline ── */
  .timeline {
    position: relative;
    padding-left: var(--space-2xl);
  }

  .timeline::before {
    content: '';
    position: absolute;
    left: 8px;
    top: 5px;
    bottom: 5px;
    width: 2px;
    background: linear-gradient(to bottom, var(--border-accent), var(--border-subtle));
  }

  .timeline-entry {
    position: relative;
    margin-bottom: var(--space-3xl);
    padding: var(--space-xl);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border-subtle);
    background: var(--surface-card);
    transition: all 0.25s ease;
    animation: fadeUp 0.6s ease-out both;
  }

  .timeline-entry:hover {
    border-color: var(--border-accent);
    box-shadow: 0 8px 24px var(--shadow-accent-soft);
  }

  .timeline-entry:first-child {
    border-color: var(--border-accent);
  }

  .timeline-dot {
    position: absolute;
    left: calc(-1 * var(--space-2xl) - 4px);
    top: var(--space-xl);
    font-size: var(--body-text);
    color: var(--accent-bright);
    line-height: 1;
  }

  .timeline-entry:first-child .timeline-dot {
    filter: drop-shadow(0 0 6px var(--moon-glow));
  }

  .timeline-head {
    display: flex;
    align-items: baseline;
    gap: var(--space-lg);
    flex-wrap: wrap;
    margin-bottom: var(--space-lg);
  }

  .timeline .version {
    font-family: var(--font-mono);
    font-size: var(--text-lg);
    color: var(--accent-bright);
    font-weight: 700;
  }

  .timeline .label {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 600;
    color: var(--text-primary);
    margin: 0;
    line-height: var(--leading-tight);
  }

  .timeline .date {
    font-family: var(--font-mono);
    font-size: var(--label-text);
    color: var(--text-muted);
    margin-left: auto;
  }

  .timeline-items {
    list-style: none;
    padding: 0;
    margin: 0;
    display: grid;
    gap: var(--space-sm);
  }

  .timeline-items li {
    font-family: var(--font-body);
    font-size: var(--body-text);
    color: var(--text-secondary);
    line-height: var(--leading-relaxed);
    padding: var(--space-xs) 0;
    padding-left: var(--space-xl);
    position: relative;
  }

  .timeline-items li::before {
    content: '';
    position: absolute;
    left: var(--space-md);
    top: 0.7em;
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: var(--accent-bright);
    opacity: 0.5;
  }

  .timeline-entry.latest {
    border-color: var(--border-accent);
    background: linear-gradient(135deg, var(--surface-card) 0%, rgba(104, 152, 190, 0.04) 100%);
  }

  .latest-badge {
    font-family: var(--font-mono);
    font-size: var(--badge-text);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--bg-deep);
    background: var(--accent-bright);
    padding: var(--space-xs) var(--space-md);
    border-radius: var(--radius-full);
    line-height: 1;
  }

  .timeline-loading,
  .timeline-error {
    text-align: center;
    padding: var(--space-2xl);
    color: var(--text-muted);
    font-family: var(--font-body);
  }

  @media (max-width: 640px) {
    .timeline { padding-left: var(--space-xl); }
    .timeline-dot { left: calc(-1 * var(--space-xl) - 4px); }
    .timeline-entry { padding: var(--space-lg); }
    .timeline-head { flex-direction: column; gap: var(--space-xs); }
    .timeline .date { margin-left: 0; }
  }
</style>
